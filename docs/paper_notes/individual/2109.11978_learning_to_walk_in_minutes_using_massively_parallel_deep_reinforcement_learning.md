# Learning to Walk in Minutes Using Massively Parallel Deep Reinforcement Learning

## Metadata

- arXiv: [2109.11978](https://arxiv.org/abs/2109.11978)
- PDF: [local PDF](../../references/papers/2109.11978_learning_to_walk_in_minutes_using_massively_parallel_deep_reinforcement_learning.pdf)
- Extracted text: [local text](../extracted_text/2109.11978_learning_to_walk_in_minutes_using_massively_parallel_deep_reinforcement_learning.txt)
- Category: 四足 RL locomotion baseline
- Reading priority: 必读，第一篇

## One-Sentence Takeaway

这篇论文是本项目的 baseline 入口。核心价值不是某个复杂网络结构，而是证明 Isaac Gym 大规模并行仿真加 PPO 可以把四足 locomotion 训练从数小时压到分钟级，并形成后来 legged_gym 工作流的工程基础。

## Problem

传统腿式机器人 RL 训练慢、环境吞吐不足、调参反馈周期长，导致 locomotion baseline 难以快速复现。

## Core Method

用 GPU 并行物理仿真同时跑大量环境，训练 command-conditioned actor-critic。策略主要输出关节位置 target offset，再由 PD 控制器落到电机层；奖励以速度跟踪、姿态稳定、能耗、动作平滑、接触行为等项组成。

## Environment / Robot / Simulator

ANYmal 类四足机器人；Isaac Gym；平地、粗糙地形和 curriculum 风格 terrain。

## What To Reuse In 2RL

2RL 的 Milestone 1 应按它的接口建立 observation/action/reward/config：先跑 flat velocity tracking，再加 rough terrain。MoE 之前不要过早引入感知、gate 或 safety，否则 baseline 不可诊断。

## Cautions

不要直接把 reward 权重当成固定真理；先复现可走，再逐项做 reward ablation。Isaac Gym legacy 路径和 Isaac Lab 现代路径要分开。

## Reading Checklist

- Identify observation space and which parts are deployable without privileged information.
- Identify action representation and low-level controller assumption.
- List reward terms and termination conditions.
- Record simulator/framework versions if the paper provides them.
- Record evaluation metrics that can map to `experiments/README.md`.
