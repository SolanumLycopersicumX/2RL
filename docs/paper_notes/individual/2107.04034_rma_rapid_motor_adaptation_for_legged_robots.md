# RMA: Rapid Motor Adaptation for Legged Robots

## Metadata

- arXiv: [2107.04034](https://arxiv.org/abs/2107.04034)
- PDF: [local PDF](../../references/papers/2107.04034_rma_rapid_motor_adaptation_for_legged_robots.pdf)
- Extracted text: [local text](../extracted_text/2107.04034_rma_rapid_motor_adaptation_for_legged_robots.txt)
- Category: Rapid Motor Adaptation
- Reading priority: 必读，第二篇

## One-Sentence Takeaway

RMA 是 history encoder 和 latent adaptation 的核心参考。它把不可直接观测的环境因素，如摩擦、负载、地形和电机状态，通过近期本体感知历史估计成 latent，再让 base policy 使用。

## Problem

真实机器人不能直接知道摩擦、质量偏差、地形参数等 privileged variables，但这些变量强烈影响动作效果。

## Core Method

两阶段训练：先训练能使用 privileged latent 的 teacher/base policy，再训练 adaptation module 从历史观测预测 latent。部署时只使用真实可观测历史。

## Environment / Robot / Simulator

腿式机器人；仿真训练，真实环境适应实验。

## What To Reuse In 2RL

2RL 的 `history_encoder.py` 和 gate input 应借鉴这个思想：gate 不只看当前 obs，也看过去若干步的动作、姿态和速度误差。

## Cautions

不要把 privileged terrain label 直接作为最终部署输入。可以先用 label 做 MoE sanity check，再 distill 到 height/history encoder。

## Reading Checklist

- Identify observation space and which parts are deployable without privileged information.
- Identify action representation and low-level controller assumption.
- List reward terms and termination conditions.
- Record simulator/framework versions if the paper provides them.
- Record evaluation metrics that can map to `experiments/README.md`.
