# RMA: Rapid Motor Adaptation for Legged Robots

## 元数据

- arXiv: [2107.04034](https://arxiv.org/abs/2107.04034)
- 本地 PDF: [local PDF](../../references/papers/2107.04034_rma_rapid_motor_adaptation_for_legged_robots.pdf)
- 提取文本: [local text](../extracted_text/2107.04034_rma_rapid_motor_adaptation_for_legged_robots.txt)
- 类别: Rapid Motor Adaptation
- 阅读优先级: 必读，第二篇

## 一句话结论

RMA 是 history encoder 和 latent adaptation 的核心参考。它把不可直接观测的环境因素，如摩擦、负载、地形和电机状态，通过近期本体感知历史估计成 latent，再让 base policy 使用。

## 研究问题

真实机器人不能直接知道摩擦、质量偏差、地形参数等 privileged variables，但这些变量强烈影响动作效果。

## 核心方法

两阶段训练：先训练能使用 privileged latent 的 teacher/base policy，再训练 adaptation module 从历史观测预测 latent。部署时只使用真实可观测历史。

## 环境 / 机器人 / 仿真器

腿式机器人；仿真训练，真实环境适应实验。

## 对 2RL 的可复用点

2RL 的 `history_encoder.py` 和 gate input 应借鉴这个思想：gate 不只看当前 obs，也看过去若干步的动作、姿态和速度误差。

## 注意事项

不要把 privileged terrain label 直接作为最终部署输入。可以先用 label 做 MoE sanity check，再 distill 到 height/history encoder。

## 阅读检查清单

- 明确 observation space，并区分哪些输入是真实部署可观测的，哪些是 privileged 信息。
- 明确 action representation 和底层控制器假设。
- 记录 reward terms、termination conditions 和 curriculum 设计。
- 如果论文给出 simulator/framework 版本，记录版本和仓库来源。
- 把 evaluation metrics 映射到 `experiments/README.md` 的实验记录格式。
